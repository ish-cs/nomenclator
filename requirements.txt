# Augur — Python dependencies
# Install with: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# Note: Homebrew Python (macOS) is externally managed (PEP 668) — use a venv, not system pip.

# Semantic search (vector store + embeddings)
chromadb>=1.0.0
sentence-transformers>=2.2.0

# Optional LLM backends for demo_agent.py (--api flag):
# pip install anthropic    → Claude API  (set ANTHROPIC_API_KEY)
# pip install openai       → OpenAI API  (set OPENAI_API_KEY, optional OPENAI_MODEL)
